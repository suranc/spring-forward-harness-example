apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-forward.fullname" . }}
  labels:
    {{- include "spring-forward.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-forward.selectorLabels" . | nindent 4 }}
